SET(PROJECT_BIN ${PROJECT_NAME})
ADD_EXECUTABLE(${PROJECT_BIN} ${MAINFOLDER}/src/main.cxx ${PROJECT_SRCS} Exception.hpp)
TARGET_LINK_LIBRARIES(${PROJECT_BIN} ${PROJECT_LIBS} ${OPENGL_LIBRARIES} ${GLFW_LIBRARIES} easylogging)
